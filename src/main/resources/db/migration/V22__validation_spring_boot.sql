-- V22__validation_spring_boot.sql

CREATE TABLE IF NOT EXISTS referentiel.validation_v22 (
                                                          id BIGSERIAL PRIMARY KEY,
                                                          created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);