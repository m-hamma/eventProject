spring:
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  application:
    name: eventProject-local

  datasource:
    url: jdbc:postgresql://localhost:5432/eventdb
    username: mohamed.hamma
    password: secret

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    org.flywaydb: INFO

server:
  port: 8080