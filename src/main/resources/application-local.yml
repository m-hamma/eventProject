debug: true
spring:
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  datasource:
    url: jdbc:postgresql://localhost:5432/eventdb7
    username: mohamed.hamma
    password: secret

  jpa:
    defer-datasource-initialization: false
    hibernate:
      ddl-auto: validate
    show-sql: true

  data:
    jdbc:
      repositories:
        enabled: true

  sql:
    init:
      mode: never